# MEMORY

Verified runtime truth below.

Verified project baseline:
- AgentTeam is a Rust-first framework project.
- Initial work must create architecture/module docs and file structure before code.
- Runtime must avoid fallback, duplicate owner logic, and cross-module internal state access.
- `TANote.md` is now specified as an agent-readable forum-style collaboration projection: agents post through CLI/daemon, TANote Board owns format/order/thread projection, and task/message/event truth remains in daemon-owned modules.
- Local AgentTeam skill must include YAML frontmatter; otherwise Codex skips configured-manager/worker CLI guidance.
- MVP debug requires Resource Lifecycle evidence: every long-lived resource has owner/scope/lease/release policy, orphan/leak/growth detection, event persistence, debug snapshot, and exact-handle cleanup.
- Debug capture is durable in v1: debug bundles/evidence/snapshots must be persisted before CLI/UI output claims them.
- Daemon/session close must run scoped resource cleanup and tracked temporary file cleanup; v1 avoids aggressive memory hard caps but forbids unbounded growth without release/drain/cleanup visibility.
- Function map is a hard implementation gate: no feature work without map owner/contracts/paths/gates and verification mapping.
- Rust source size policy: hand-written Rust leaf files have a 500-line hard limit; shared blocks/contracts are not exempt and should be split by owner/domain/node.
- Local skill split: `agentteam` is the runtime/CLI collaboration surface; `agentteam-dev` is the repository module-development cycle skill.
- Daemon domain design: zterm may expose multiple daemons, so AgentTeam agent names are domain-local. Cross-daemon addressing uses Daemon Domain Registry and canonical `agent@domain`; Communication Center must consume route plans instead of parsing domain strings itself.
- tmux loopback MVP truth: `agentteam tmux loopback --runtime-home <path> --session-count <n> --json` is the first verified multi-session adapter smoke. It creates managed `TA-...` sessions through `agentteam-tmux`, uses `sh -lc` to keep the session process alive, captures pane output with `tmux capture-pane -pJ` so wrapped markers stay matchable, and cleans exact session handles only. Verified with 2 sessions and no residual `TA-agentteam-tmux-smoke-loopback*` sessions.
- Communication Center truth: comm now owns typed message, broadcast, ready report, task-board query, and task claim routing envelopes. `agentteam-comm` validates and accepts those requests without owning task state, and `agentteam-contracts::comm` carries the adjacent-node contracts for the same routes. Verified by `cargo xtask verify` after splitting comm tests out of `comm/mod.rs` to keep every hand-written Rust leaf file under 500 lines.
- Phase 0/Phase 1 MVP scaffold truth: `cargo xtask red-tests` now performs static scans for red-test plan coverage, broad kill patterns, agent-facing transport internal leaks, TOML parsing outside Config Center, state-file writes outside Persistence, domain parsing outside Daemon Domain Registry, non-adjacent `From/TryFrom` pipeline conversions, and contract feature_id coverage.
- Function map gate truth: every Rust function or method under `crates/` and `xtask/src/` must have a matching function-level registry entry in `docs/architecture/function-map.md`; otherwise `cargo xtask verify-function-map` fails. The canonical `cargo xtask verify` runs function-map verification before compile/test gates.
- Phase 1 contracts truth: `agentteam-contracts` now owns typed MVP nodes for config, domain, persistence, error, debug, and resource chains with adjacent-node builders and 13 unit tests. This is contract scaffold only, not business runtime behavior.
- Phase 2 Config Center truth: `agentteam-config` is the only crate with TOML parsing, using `toml::from_str` inside `parse_config_toml`. It loads explicit config paths, parses `docs/config/config.toml.example`, validates project/team/domain/member rules, normalizes runtime config, and creates redacted config snapshots. Verified by `cargo xtask verify` with 7 Config Center unit tests.
- Phase 3 Daemon Domain Registry truth: `agentteam-runtime::domain` is the only owner that parses domain-qualified targets. It registers one local domain plus remote domains, enforces id/alias uniqueness, resolves bare targets to local only, resolves `agent@domain`, `role:<role>@domain`, `team:<team>@domain`, and `all@domain`, returns local/remote route plans, and rejects unknown remote domains without fallback. Verified by `cargo xtask verify` with 7 runtime domain tests.
- Phase 4 Persistence Event Log truth: `agentteam-persist` is the only state-file writer owner for MVP event JSONL. It appends typed event records, returns `PersistResp03AppendReceipt`, replays from zero or a sequence, materializes latest sequence into `PersistResp05MaterializedState`, and fails explicitly on corrupt JSONL, sequence mismatch, or empty event drafts. Verified by `cargo xtask verify` with 7 Persistence unit tests.
- Phase 5 Error Center truth: `agentteam-error` exposes `handle_framework_fault` as the public framework-fault entrypoint. It classifies fault facts, generates `<module>.<class>.<specific>.<time>.<seq>` codes, creates independent `evidence_id`, persists a framework error event through `agentteam-persist`, then returns `TeamErr04Projection`. Normal `task.agent_task_error` is rejected as Task Engine state, and persistence failure does not project success. Verified by `cargo xtask verify` with 6 Error Center unit tests.
- Phase 6 Debug/Resource truth: `agentteam-resource` owns MVP resource leases and persists acquire/release/leak events through Persistence; `agentteam-debug` owns debug bundle capture and must acquire/release a `debug_bundle` lease, include a public Resource snapshot, persist the debug bundle event, and return `DebugResp03Bundle` with persistence receipt and resource snapshot id. Verified by `cargo xtask verify` with 5 Resource tests and 3 Debug tests.
- Local CLI parsing truth: `agentteam-cli` must delegate argv semantics to `agentteam-gateway`; Gateway owns local parsing for `config check`, `domain resolve`, and `debug snapshot` into `TeamReq03ValidatedIntent`. Current CLI output is parse-only and explicitly includes `local_parse_only: true`; it must not claim Config/Domain/Debug business execution until owner APIs are wired behind the intent.
- Local CLI owner execution truth: `agentteam-cli` now parses through Gateway, executes `TeamReq03ValidatedIntent` through `agentteam-runtime::local::execute_local_intent`, and renders through Output Gateway. `config check` calls Config Center, `domain resolve` builds Domain Registry from Config Center validated user config and calls Domain Registry resolution, and `debug snapshot` persists resource/debug events under the requested runtime home. Verified by `cargo xtask verify` plus three CLI smokes; no daemon/tmux/zterm startup is implemented in this slice.
- Daemon check skeleton truth: `agentteam daemon check --config <path> --json` is a local routeability smoke only. It validates config through Config Center, builds Domain Registry through runtime orchestration, returns routeable endpoint count and explicit `not_started_by_check` / `not_touched_by_check` statuses, and does not start daemon/tmux/zterm. Verified by `cargo xtask verify` plus CLI smoke.
- Phase 8 Task Engine local truth: `agentteam-runtime::task` owns MVP task event state. Local CLI supports `task send/list/status/done/error` with `--runtime-home`, persists task events through `agentteam-persist`, and reconstructs board/status by replaying `events/agentteam.jsonl`. `task error` is normal task state, not Error Center framework fault. Verified by `cargo xtask verify` and CLI smoke (`send -> list -> done -> status`) writing two task events under `target/agentteam-task-smoke`.
- Task Engine claim truth: `TaskEngine::claim_task` now selects eligible queued tasks by assigned-first, blocked-first, then priority and age, persists `task_claimed`, and replays `priority/blocked` through the task board. Gateway/CLI now expose `task claim --runtime-home ... --worker-name ... --worker-role ... --json`, and the local smoke verifies claim -> running -> done on the same task. Verified by the full gate set after the claim slice.
- Communication Center delivery persistence truth: `agentteam-comm` now owns a replayable `persist_delivery_event` helper that appends JSONL delivery events through `agentteam-persist`, classifies append failures as comm persistence errors, and keeps route validation separate from persistence. Verified by `cargo test -p agentteam-comm -p agentteam-persist`, `cargo xtask verify-function-map`, `cargo xtask red-tests`, `cargo xtask verify-code-size`, and `cargo xtask verify`.
- Communication Center message-send truth: `comm.send_message` plus `agentteam msg send --runtime-home ... --from ... --to ... --action ... --body ... --json` now validate the target, persist a `comm_message_delivery` event, and return replayable delivery metadata (`event_id`, `sequence`, `log_path`). Verified by `cargo test -p agentteam-comm -p agentteam-gateway -p agentteam-runtime`, `cargo xtask red-tests`, `cargo xtask verify-code-size`, `cargo xtask verify-function-map`, and `cargo xtask verify`.
- Communication Center ready-report truth: `comm.send_ready_report` plus `agentteam ready report --runtime-home ... --sender ... --team ... --agent-name ... --body ... --json` now validate the worker ready report, persist a `comm_ready_report_delivery` event, and return replayable ready-report metadata (`event_id`, `sequence`, `log_path`). Verified by `cargo test -p agentteam-comm -p agentteam-gateway -p agentteam-runtime`, `cargo xtask red-tests`, `cargo xtask verify-code-size`, `cargo xtask verify-required-files`, `cargo xtask verify-skill-frontmatter`, `cargo xtask verify-resource-lifecycle`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo xtask verify-function-map`, and `cargo xtask verify`.
- Communication Center broadcast truth: `comm.send_broadcast` plus `agentteam msg broadcast --runtime-home ... --sender ... --team ... --action broadcast --body ... --members ... --json` now validate broadcast members, persist a `comm_broadcast_delivery` event, and return replayable broadcast delivery metadata (`event_id`, `sequence`, `log_path`). Verified by `cargo xtask verify-function-map`, `cargo xtask verify-code-size`, `cargo xtask red-tests`, `cargo xtask verify-required-files`, `cargo xtask verify-skill-frontmatter`, `cargo xtask verify-resource-lifecycle`, `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo xtask verify`, and a playground smoke in `~/code/playground`.
- Gateway size truth: `crates/agentteam-gateway/src/input.rs` was split with `broadcast.rs` and `options.rs` so every hand-written Rust leaf file stays under the 500-line cap.
- Playground smoke truth: `~/code/playground` can run the minimal local smoke with runtime home `/Volumes/extension/code/playground/agentteam-smoke`; ready report, broadcast, task send, task claim, task done, and task status all returned JSON receipts with persisted event ids/sequences.
- Real E2E smoke truth: `/Users/fanzhang/Documents/github/agentteam/agentteam-e2e` and `/Users/fanzhang/Documents/github/agentteam/agentteam-tmux-e2e` passed a full local sequence of ready report, direct message, broadcast, task send/list/claim/done/status, second task claim/error/status, and tmux loopback; the tmux loopback returned `observed_count=2`, `cleaned_handle_count=2`, and `cleanup_status=cleaned_exact_handles`.
- Unified usage guide truth: `docs/usage/agentteam-usage.md` is the role-based operating guide for the configured manager, workers, and operators, and it is linked from the `agentteam` skill and CLI/startup docs.
- Startup truth: standard tmux is the transparent bootstrap carrier; startup parameters assign the configured manager identity, and the manager later initializes workers with their own tmux + role params through skills/CLI. User-facing docs no longer describe routecodex/rcc as the bootstrap path.
- Bootstrap refinement truth: the configured manager is the only first-launch target for startup params; workers are launched later by the manager with their own tmux + role params through skills/CLI.
- Manager output/status truth: a live configured-manager session that has received a request but has not yet produced a semantic reply must be treated as `busy`/pending, not `error`, as long as the session remains alive and no transport/session/framework fault appears. `stdout` is transport evidence, not final status truth.
- Manager test evidence retention truth: configured-manager test sessions and their evidence must not be cleaned during the test run; cleanup is deferred until the user explicitly exits the manager so failure evidence remains available.
- Root-manager truth: the configured manager is the root manager for the project agent tree. It must not auto-exit after startup; user exit triggers scoped worker exit through the shutdown flow. Manager skills must include a CLI feedback path that returns execution results to the framework and closes the loop.
- Control-plane split truth: `agentteam-runtime::local.rs` now stays under the 500-line leaf-file cap by delegating control actions to `agentteam-runtime::control`; `AgentControlCenter` implements `Default` so clippy stays clean. Verified with `cargo fmt --check`, `cargo xtask verify-required-files`, `cargo xtask red-tests`, `cargo xtask verify-function-map`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo xtask verify`.
- Manual control smoke truth: `cargo run -p agentteam-cli -- control attach --agent Kevin --team demo --session TA_control_smoke --json` returned a busy attach projection against a live tmux session, and exact `tmux kill-session -t TA_control_smoke` cleanup removed the session afterward.
- Codex SDK bridge truth: `agentteam-control` now launches the local Codex Python SDK source bridge from `/Users/fanzhang/code/codex/sdk/python/src` with explicit env vars `AGENTTEAM_CODEX_SDK_SRC` and `AGENTTEAM_CODEX_BIN`. Bridge session state is project-scoped under `~/.agentteam/sessions/<project_slug>/headless/<session_name>/state.json`. Verified smoke on `TA_smoke_codex_bridge_3`: `start` succeeded and returned idle, `status` read the persisted bridge state and returned idle, and `run` on the reused idle thread returned an explicit SDK `thread not loaded` error instead of a silent fallback. This means start/status are now wired, but reused-idle-thread run still needs a persistent-load strategy before headless turn execution can be claimed complete.

- Persistent Codex SDK headless bridge truth: Agent Control Center now owns a session-scoped Python bridge process per headless session, with Rust requests over a local JSON socket and state evidence under `~/.agentteam/sessions/<project_slug>/headless/<session_name>/state.json`. Real CLI smoke on `TA_headless_bridge_smoke_4` passed: `control headless-run` returned `details=ready`, `control headless-status` returned `idle/thread idle`, `control headless-stop` returned ok, and exact PID check showed no remaining `headless_bridge.py` process. The bridge resumes persisted `thread_id` when a new bridge process is required, so status/run do not depend on stale in-memory state only. Verified by `cargo xtask verify`.
- Headless agent create/control/recover truth: `control headless` is the MVP create/bind action, `control headless-run` is the control input path, and `control headless-status` after scoped `headless-stop` recovers by spawning a new bridge process while resuming the same persisted `thread_id`. Normal scoped stop projects `offline`, not `error`. Verified on `TA_headless_create_recover_offline_20260609`: stop removed PID 17280, recovery used new PID 18524 with the same `thread_id` `019eaaae-9013-76c0-805c-b9ae0c60c8ad`, and final scoped stop returned `offline`.
- Minimal sample manager/Alice workflow truth: for headless workflow smokes writing playground runtime state, run the headless control command from `~/code/playground` so Codex SDK workspace-write sandbox covers the runtime home. Verified on `/Users/fanzhang/code/playground/agentteam-workflow-20260609-03`: ready report, message, task create, task claim, Alice headless CLI completion, final `task.status=done`, and scoped `headless-stop=offline` all passed with event sequences 1-5.
- Naming correction truth: `Kevin` is only the configured/default sample agent name. Code functions, structs, schema fields, feature ids, function ids, and red-test ids must use neutral manager/root-manager/configured-agent naming; the red-test gate enforces this boundary.
- Visible TUI input verification truth: prompt text visible inside a Codex TUI input box is only staged input, not submitted agent work. For attach_tui delivery evidence, require literal input plus Enter and then observe a submitted Codex turn/output through `control observe` or `tmux capture-pane`. The current tmux adapter `send_input` already sends literal input and Enter; E2E proof must not rely on input-box visibility alone.
- Startup skill install truth: `agentteam start` / `start worker` install the local AgentTeam skill into the target cwd before prompt seeding and expose `skill_install_status`, `skill_path`, and `cli_path`. Agent prompts/env include `AGENTTEAM_CLI` and `AGENTTEAM_SKILL_PATH` so worker agents outside the repo can still read skills and run the framework CLI.
- Attach TUI SDK status truth: for Codex SDK-seeded visible TUI sessions, `control status` accepts `--cwd` and `--project` and merges tmux observation with SDK session status. Verified on `TA_local_agentteam_e2e_tui_20260609_01_Kevin`: projection stayed `attach_tui` and details included `sdk_status=status; sdk_details=thread idle; tmux_observed=true`.
- Attach TUI SDK status hardening truth: when `control status` is called with SDK scope, Control Center requires an existing persisted Codex thread binding and fails explicitly if binding/status is missing; it must not downgrade to stdout-only status. Without SDK scope, status says `sdk_status=not_requested` and is tmux-only evidence. `send`/`pause`/`stop` stay transport-action projections and are not overwritten by SDK idle. Verified with positive SDK-status smoke, negative missing-binding smoke, red-test scanner, and `cargo xtask verify`.
- Test resource cleanup truth: non-reusable TA smoke/probe/E2E sessions may be cleaned after user authorization by exact `tmux kill-session -t <name>` plus scoped `control headless-stop` for bridge processes. After the 2026-06-09 cleanup only reusable `TA_local_agentteam_Kevin` remained and no `headless_bridge.py` process was present.
